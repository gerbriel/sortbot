# Python build and tool output. None of this belongs in the repo.
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# `cp fly.toml.example fly.toml` and edit — the real one names your app and is
# yours, not the repo's.
fly.toml

# Output of `python -m app.cli --out ...` when run in place.
out/

# Local deploy secrets (see deploy.sh). Never committed.
.env.deploy
.env
.env.*
