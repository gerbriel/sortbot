# 02 — latent defect review: implementation log

Source: `docs/reviews/02-debugging.md`. Every CONFIRMED finding assigned to this pass was
implemented; 17 and 18 belong to the performance pass and 21 was PLAUSIBLE-only.
Nothing was committed.

Legend for "nuance": things a reviewer must know because the behaviour changed in a way
the finding text does not spell out.

---

## Per-finding

| # | Status | Files + lines | Test | Nuance |
|---|---|---|---|---|
| 1 | **Fixed** | `src/lib/productService.ts:572-634` (`updateProduct` now takes an optional `userId`, chains `.select('id')`, treats 0 rows as failure and recovers by upserting the row); patch-building extracted to `buildProductPatch` `:467-570`. Caller: `src/components/ProductDescriptionGenerator.tsx:118-127,143,284,1246,1703` (a `userIdRef` fed from the session, threaded into all four Step-3 saves). | `src/lib/productService.test.ts` — 5 tests (`0 rows is a failure, not a success`) | The recovery upsert deliberately carries **no `batch_id`** (§18 #3), so a row it creates starts unassigned and is picked up by the normal upload/registration path. `updateProduct`'s signature gained a 3rd optional arg — all existing call sites still compile. |
| 2 | **Fixed** | Write side: `src/lib/productService.ts:636-687` (`syncGroupFieldsToDatabase` resolves the **leader** — `id === productGroup` — writes it through the checked `updateProduct`, then mirrors the same patch onto the remaining members in ONE `.in('id', …)` request) and `:351-359` (`saveBatchToDatabase` keys the group row on the leader, not `groupItems[0]`). Read side: `src/App.tsx:2386-2394` (`productsByGroup` now prefers the leader row over "earliest created_at"). | `src/lib/productService.test.ts` — 6 tests (`writes the key the restore path reads`) | Two behaviour changes: (a) the function now returns `Promise<boolean>` instead of `void` (all callers either ignore it or `.catch()`); (b) a group of N photos now takes **2** requests per save instead of 1, and every member row ends up holding the group's fields. That redundancy is the point — no future re-grouping can strand the text on an unread row. Legacy fresh-UUID groups with no leader fall back to member[0], exactly as before. |
| 3 | **Fixed** | `src/components/ImageUpload.tsx:436-466` (cancel deletes `product_images` then `products` for every item it had already emitted — chunked 100 — **before** removing the storage objects, then chunks the storage removal, then calls the new `onUploadCancelled`); prop declared `:66-72`; wired in `src/App.tsx:503-524` (`handleUploadCancelled`) and `:2793`. | — (needs a component harness; the DB-order logic is 15 lines of straight-line code inside the upload loop) | Rows are deleted before files on purpose: if the row delete fails the files still exist and the situation is recoverable. `filterUnreferencedStoragePaths` is intentionally **not** used here — these paths were minted seconds ago by this very run and cannot be shared. STILL OPEN (not in the finding's fix): cancelling during chunk 1 leaves the pre-inserted `workflow_batches` row as an empty ghost batch. |
| 4 | **Fixed** | `src/lib/workflowBatchService.ts:263-300` — the `product_images` lookup is chunked at 100 and its error is destructured into an `imageLookupComplete` flag. | `src/lib/workflowBatchService.test.ts` — `chunks the product_images lookup at 100…`, `removes the storage files it found`, `leaves storage untouched when the image lookup could not be completed` | When the lookup is incomplete we now delete the DB rows but leave **storage untouched** rather than guessing — a leak is recoverable, a wrong delete is not. Also widened the `originalStoragePath` scrape to all four `workflow_state` arrays (it only read `uploadedImages`/`groupedImages`, which the current save format always leaves empty — the same dead-array bug as finding 13). |
| 5 | **Fixed** | `src/lib/workflowBatchService.ts:300-390` — new order: read + reference-count → claim (verified) → **confirmed** `workflow_batches` delete → `product_images` → `products` → storage. | `src/lib/workflowBatchService.test.ts` — `deletes NOTHING when the batch row cannot be claimed`, `…when the batch-row delete affects 0 rows`, `confirms the batch row is gone BEFORE it destroys anything else`, `keeps a storage file another batch still references`, `refuses when there is no authenticated user` | `safePaths` is computed in step 1, while the rows still exist (§18 #15 — calling `filterUnreferencedStoragePaths` after the delete makes every file look unreferenced); only the `storage.remove()` moved to the end. A failed claim is now a hard `false` with zero side effects, so the Library's red error banner is honest. Residual risk, unchanged in kind: if the `products` delete fails *after* the batch row is gone, `deriveLibraryData` will still synthesise a batch from the orphaned products — now logged as a warning. |
| 6 | **Fixed** | `src/App.tsx:1653-1668` (`handleImagesSorted`) and `:1783-1793` (`handleImagesGrouped` debounced upsert) — `batch_id` removed from both payloads. | covered indirectly by the productService `batch_id` assertion; not directly testable without a component harness | `user_id` was **kept** (the review suggested dropping both): these upserts run with `ignoreDuplicates: false` and must still be able to CREATE a row, and `user_id` is NOT NULL + RLS-checked on insert. Rewriting `user_id` to the current editor is what Library's delete already does deliberately; `batch_id` was the theft vector. Consequence to watch: a row these upserts create now starts with `batch_id = null` and shows up in Library's "Unassigned" section until the upload/registration path assigns it — visible and deletable, versus the previous silent cross-batch theft. |
| 7 | **Fixed** | `src/App.tsx:2144-2165` — the ±24 h orphan query gained `.is('batch_id', null)`, `.eq('user_id', <current user>)` (only when the id is known) and `.limit(max(items*2, 100))`. | — (inside `handleOpenBatch`) | Kept the full `slimProductSelect`: the matched rows feed the same field merge, so narrowing the select would have silently dropped data. Only genuinely unassigned rows can now be adopted, which also closes the entry point that made finding 6's theft reachable. |
| 8 | **Fixed** | `src/lib/tusUpload.ts:46-149` — the `async` Promise executor is gone; the async body is an inner IIFE with `.catch(reject)` and an explicit try/catch around `getSession()`. Caller watchdog: `src/components/ImageUpload.tsx:497-509` + `TUS_TIMEOUT_MS` `:28-31`. | `src/lib/tusUpload.test.ts` — 2 tests, incl. a 250 ms race that fails loudly if the promise ever hangs again | The watchdog rejects into the existing `catch (tusErr)`, so a wedged TUS session now falls through to the standard Supabase PUT instead of stalling the whole chunk (and therefore the whole batch). |
| 9 | **Fixed** | `src/lib/productService.ts:672-716` (new `flushProductPatchKeepalive` — a `keepalive` PATCH straight at PostgREST) + `src/lib/imageRowSync.ts:43-49` (`stage4ColumnsKnownAvailable`, the synchronous view of the cached probe); consumer `src/components/ProductDescriptionGenerator.tsx:300-345`. | `src/lib/productService.test.ts` — 2 tests (`survives page teardown`); `src/lib/imageRowSync.test.ts` — probe default | `sendBeacon` cannot send PATCH and PostgREST has no POST-shaped update, so `fetch(..., {keepalive:true})` is the only option. The flush writes the **leader** row only (finding 2) and needs no `await`, which is why the access token is mirrored into a ref by a new auth effect. Stage 4 columns are omitted unless the probe already resolved positively — always safe. |
| 10 | **Fixed** | `src/components/ProductDescriptionGenerator.tsx:106-120` (`groupSaveTimerRef` + `directSaveTimerRef` replace the single shared `productSaveTimerRef`), `:147`, `:275-290`, `:300-345` (unload drains `pendingSaveGroupRef`). | — (timer interaction needs a component harness) | `debouncedDirectSave` now actually fires, so a voice-table edit produces **two** saves of the same group: the 500 ms store-driven one and the 800 ms direct one. That is the intended behaviour from the review (the direct path exists because it carries the just-typed value even if the store hasn't propagated); combined with finding 2's mirror that is up to 4 requests per edit burst. Flagged for the performance pass, not reduced here. |
| 11 | **Fixed** | `src/components/ImageUpload.tsx:241-270` — the concurrency guard moved above `onUploadStart()` and the fingerprint cleanup; the cleanup now only drops `tus::` entries older than 24 h (or unparseable ones). | — | This makes `storeFingerprintForResuming` functional for the first time: an interrupted upload can now resume on the next visit. That is the advertised behaviour, but it is behaviour that has never actually run in production — worth a manual "drop, kill the connection, re-drop" smoke test. |
| 12 | **Fixed** | `src/components/ImageUpload.tsx:405-417` — the per-chunk `product_images` insert uses `buildProductImageRow`. | `src/lib/imageRowSync.test.ts` (existing row-shape tests already lock the builder) | `original_name`, `alt_text`, `transforms` and (post-migration) `captured_at` are now written at upload time as `d0147f0` intended; the later `ignoreDuplicates:true` upsert in `handleImagesUploaded` stays a harmless no-op. `position` is 0 here and in the two App-side per-item writes (`src/App.tsx:566-577`, `:1514-1520`, `:1810`) — it used to be the array index, which was meaningless for one-image-per-product rows and actively fought finding 16's merge. |
| 13 | **Fixed** | `src/lib/libraryService.ts:380-412` — `duplicateBatch` copies `processedItems` (with the same fallback chain the restore uses) and recomputes `total_images` / `product_groups_count` / `categorized_count` / `processed_count` from what it actually copied. | `src/lib/libraryService.test.ts` — 3 tests | The copy is written into `workflow_state.processedItems`, which is the slot `handleOpenBatch` reads first, so a duplicate now opens with its images. `current_step` still resets to 1 (unchanged). |
| 14 | **Fixed** | `src/App.tsx:1091-1105` (startup EXIF rescan) and `:2576-2590` (`handleOpenBatch` EXIF rescan) — the auto-save moved out of the `setProcessedItems` updater. | — | Safe because `processedItemsRef` is a `liveArrayRef` view: `.current` reads the store immediately after the setter, not on the next render. |
| 15 | **Fixed** | `src/lib/orgService.ts:62-78` — the module-level in-flight promise became a `Map` keyed by `user.id`. | `src/lib/orgService.test.ts` — 4 tests | Same-user dedupe (the StrictMode case it was written for) is preserved and asserted. |
| 16 | **Fixed — delete phase KEPT** | `src/lib/imageRowSync.ts:96-189` (new `mergeProductImageRows` + `ExistingProductImageRow`); `src/App.tsx:586-660` (`registerItemsInDB` reads the full existing row set, merges, then wipes and re-inserts). | `src/lib/imageRowSync.test.ts` — 6 merge tests, incl. the group-leader collapse reproduction | Solved the way the brief asked: the **delete stays** (§18 #3) and every still-valid row is re-inserted with its position. Merge rules: a computed row replaces the existing row for the **same `storage_path`** in place (so a regenerated CDN URL keeps its slot instead of jumping to primary), then by `image_url`; a leftover row pointing at a file we just re-wrote under a different URL is the stale row the wipe exists for and is dropped; everything else is carried forward; positions are renumbered 0..n-1 in that order. Also: if the existing-row read fails, we **skip the wipe entirely** and only upsert our own rows — never destroy what we could not read. Cost: one extra chunked SELECT per open (it replaces the previously-conditional `original_name` read). |
| 17 | **Skipped** | — | — | Owned by the performance pass (localStorage backup throttling). |
| 18 | **Skipped** | — | — | Owned by the performance pass (log storm). |
| 19 | **Fixed** | `src/App.tsx:241-245` (`orgResolvedFor` state), `:1170-1176` (set inside the `ensureOrganization` resolution), `:1202-1215` (pageview effect depends on `user?.id` + `orgResolvedFor`). | — | `betaWaitlist === null` is ambiguous (it means both "not waitlisted" and "not resolved yet"), so gating on it alone would have suppressed the `app` pageview forever. `orgResolvedFor` is set inside the async resolution and never reset, so no synchronous setState was added to any effect body (react-hooks v7 rules). |
| 20 | **Fixed** | `src/lib/productService.ts:168-173` — `price: typeof product.price === 'number' ? product.price : null`. | `src/lib/productService.test.ts` (patch mapping) | A real `0` is still written as `0` (and still blocked by the export price gate, as the review wanted). `null` lets the restore merge's `p.price ?? item.price` fall through to the in-memory value instead of pinning a fake $0. |
| 21 | **Skipped** | — | — | PLAUSIBLE only, per the brief. |
| 22 | **Fixed** | `src/App.tsx:233-241` (module-level `startupRestoreInFlight`), `:686-691` (guard), `:1146` (release). | — (dev-only StrictMode path) | Module-level, not a ref, so the second mount sees the first mount's flag — same rationale as `isOpeningBatchRef`. The flag is released at the end of the success path rather than in a `.finally()` on the chain, because a `.finally()` would also fire for the early-returning second invocation and clear the lock under the first. The only statements outside the existing inner try/catch are a `localStorage.getItem` and a log call. |
| — | **Removed** | `src/App.tsx` — the one-time `sortbot_orphan_cleanup_v3` effect (three unscoped `delete().like('storage_path','%prefix%')` statements) is gone, per the brief. | — | Under org RLS those statements could reach another workspace's rows. The effect has already run in every existing browser (the localStorage key is set), so removing it changes nothing for existing users and protects new ones. The `sortbot_orphan_cleanup_v3` key itself is simply left behind, unread — no other code touches it. |

## New test infrastructure

`src/lib/testing/supabaseMock.ts` (test-only, imported by no app code) — a chainable
`supabase` stand-in that records table/op/filters/payload per call and exposes
`inSizes()` / `callsFor()` helpers, so query **order** and **chunking** can be asserted.
Used via `vi.mock('./supabase', async () => …)` in the five service test files. It also
fakes `auth.getUser/getSession/onAuthStateChange` and `storage.remove/getPublicUrl`.

## Gates

| Gate | Before | After |
|---|---|---|
| `npx eslint . --ignore-pattern 'src/components/ui/**'` | **311 problems** | **302 problems** (my files net **−8**; the residual delta is another agent's concurrent work in `CrmPanel.tsx` / `mailto.ts` / `csvExport.ts`, which this pass did not touch) |
| per-file lint on every file I touched | App 66, PDG 24, ImageUpload 5, the six lib files 37 | App 66, PDG 24, ImageUpload 5, the six lib files **30** — no finding on a line I added; the seven new files are at **0** |
| `npm test` | 305 passed / 24 files | **357 passed / 29 files** (+40 tests from this pass: productService 15, workflowBatchService +9, imageRowSync +7, orgService 4, libraryService 3, tusUpload 2; the rest is another agent's `mailto.test.ts` / `csvExport.test.ts`) |
| `npm run build` | clean | **clean** (`tsc -b && vite build`, ✓ built). It transiently failed mid-pass on `src/components/OrgPanel.tsx:870-876` — a syntax error from another agent's in-progress edit, in a file this pass never opened; that agent has since fixed it. |

## For the orchestrator to double-check

1. **`src/components/OrgPanel.tsx`, `CrmPanel.tsx`, `csvExport.ts`, `supportService.ts`, `mailto.ts` and the `security_*.sql` migrations are another agent's concurrent work**, not this pass — the global lint/test deltas include them.
2. **Finding 6's `user_id` decision** (kept, `batch_id` dropped) is a deliberate departure from the review's proposed patch — see the nuance column. If you disagree, the alternative needs a pre-check query so these upserts never insert.
3. **Finding 10 now issues more requests per edit** (two debounce paths × the finding-2 mirror). Correct, but the performance pass should look at collapsing them.
4. **CLAUDE.md updates the orchestrator owns:** §14 #1 (`registerItemsInDB` no longer destroys group image rows), §16 "Photo reorder persistence — Missing" (the destroying half is fixed; writing a user's reorder back to `product_images.position` is still not implemented), §15/§18 (the `sortbot_orphan_cleanup_v3` effect is gone), and the new `src/lib/testing/supabaseMock.ts` + six test files in §3's coverage list.
